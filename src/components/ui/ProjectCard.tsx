import React, { ReactElement } from "react";
import tw, { styled } from "twin.macro";

type Props = {};

function ProjectCard({}: Props): ReactElement {
  return <Container></Container>;
}

type ContainerProps = {};
const Container = styled.div<ContainerProps>``;

export { ProjectCard };
